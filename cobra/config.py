# ============================================================
# CONFIGURATION — change these to match your PostgreSQL setup
# ============================================================
DB_CONFIG = {
    "host":     "localhost",
    "database": "etl_demo",
    "user":     "jacob",  # ← the one from whoami
    "password": "",                    # ← leave blank
    "port":     5432
}

CSV_FILE = "cobra_data.csv"